import 'package:flutter/material.dart';

class HouseHoldPage extends StatelessWidget {
  const HouseHoldPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('HouseHold Page'),
      ),
    );
  }
}
