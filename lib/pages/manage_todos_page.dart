import 'package:flutter/material.dart';

class ManageTodosPage extends StatelessWidget {
  const ManageTodosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Manage TODOs Page'),
      ),
    );
  }
}
